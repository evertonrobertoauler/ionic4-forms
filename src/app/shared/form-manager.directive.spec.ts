import { FormManagerDirective } from './form-manager.directive';

describe('FormManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new FormManagerDirective();
    expect(directive).toBeTruthy();
  });
});
