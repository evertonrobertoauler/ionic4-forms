import { NextInputDirective } from './next-input.directive';

describe('NextInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NextInputDirective();
    expect(directive).toBeTruthy();
  });
});
